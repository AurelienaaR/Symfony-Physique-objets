{% extends "SitephysPhysmvcBundle::layout.html.twig" %}

{% block title %}
  Modifications de l'élément {{ phys.title }}. 
{% endblock %}

{% block sitephysphysmvc_section %}
  <h2 style = "color:#FFF; text-align:center; margin:10px;">Modifications de l'élément {{ phys.title }} </h2>
{% endblock %}

{% block sitephysphysmvc_image %}
<div class="containerthome">
    <img src="{{ asset('bundles/sitephysphysmvc/images/fond-cosmo.jpg') }}" class="homephoto" alt="Image de élément" />
  </div>
{% endblock %}

{% block sitephysphysmvc_body %}
  <div class="well">
  
    <section id="descriptions">
      <h2>Descriptions de la modification de {{ phys.title }}</h2><br />

      <nav id="tabs-nav" class="clearfix">
        <a href="#domaine">Domaine et thème</a>
        <a href="#niveau">Niveau</a>
        <a href="#contenus">Contenu</a>
        <a href="#evaluation">Evaluation</a>
        <a href="#symbolisation">Symbolisation</a>
        <a href="#auteurs">Création</a>
      </nav>

      <div id="tabs-container" onload="initTab()">
        <article id="domaine">
          Domaine : {{ domaincontent }}
          <br />Thème : {{ topiccontent }}
          <br />Mode du thème : {{ topicmode }} <br />
        </article>

        <article id="niveau">  
          Niveau de base <i>(global)</i> : {{ physleveltab[0] }} 
          <br />Niveau de précision <i>(élément)</i> : {{ physleveltab[1] }} 
          <br />Description du niveau : {{ levelcontent }} <br />
        </article>

        <article id="contenus">
          Contenu (actuel) :
          <br /> {{ phys.content }} <br />
          Contenus (proposés à la modification) : 
          {% for physupdx in physupd %}
            <br />Modification proposée : {{ physupdx.title }}
            <br />Contenu : {{ physupdx.content }}
          {% endfor %}
        </article>

        <article id="evaluation">
          Evaluation : {{ phys.evaluation }} <br />
        </article>

        <article id="symbolisation">
          {% set cptsym = 0 %}
          {% for symcontent in symbolizationcontent %}
            {% set cptsym = cptsym + 1 %}
            Symbolisation {{ cptsym }} : {{ symcontent }} <br />
          {% endfor %}
        </article>

        <article id="auteurs">
          Par (actuel) {{ phys.author }}, le {{ phys.date|date('d/m/Y H:i:s') }}. 
          {% for physupdy in physupd %}
            <br />Modification proposée : {{ physupdy.title }}
            <br />Par {{ physupdy.author }}, le {{ physupdy.date|date('d/m/Y H:i:s') }}.
          {% endfor %}
        </article>
      </div>
    </section>

  </div>

{% endblock %}
